Command to enable editing or disable read only mode : open Bash and type : git config core.fileMode false
to build the project (cmake) : 
windows : cmake -B .\Build\
Linux : rm -rf build && mkdir build && cd build && cmake .. && make
